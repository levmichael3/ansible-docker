version: "2"
settings:

  conductor:
    base: ubuntu:16.04
    roles_path:
      - ./roles/
  {% if env %}
  vars_files:
    - variable_files/{{ envs }}.yml
  {% endif %}  
  project_name: mariadb

services:
  mariadb_container:
    roles:
      - role: mariadb_role  
registries: {}
